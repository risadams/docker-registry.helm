# Scenario: reference an externally-managed Secret.
# Functional goal: the chart does NOT create its own Secret, the pod consumes
# the pre-created Secret named below, and the registry comes up healthy. The
# integration harness creates the "drtest-existing-secret" Secret (with the
# required haSharedSecret key) before installing.
existingSecret: drtest-existing-secret
